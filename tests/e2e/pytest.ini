[pytest]
testpaths = .
addopts = -ra --strict-markers --base-url=http://localhost:4200
markers =
    e2e: Playwright end-to-end tests requiring the live stack on 8000/4200
filterwarnings =
    ignore::DeprecationWarning
