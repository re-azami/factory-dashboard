[pytest]
testpaths = .
addopts = -ra --strict-markers --base-url=http://localhost:8501
markers =
    e2e: Playwright end-to-end tests requiring the live stack on 8000/8501
filterwarnings =
    ignore::DeprecationWarning
