[project]
name = "factory-dashboard-frontend"
version = "0.1.0"
description = "Streamlit chat UI for the factory dashboard MVP"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40",
    "httpx>=0.27",
]
