"""Authentication and authorization (users, permissions, password hashing).

This package owns the auth tables and helpers. AUTH-009 will flesh out a
proper permission-registry helper; for now [permissions.py](permissions.py)
just lists the canonical names that the admin-seed routine grants.
"""
